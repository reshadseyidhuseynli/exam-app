spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exam
    username: root
    password: 12345
    hikari:
      minimum-idle: 5
      maximum-pool-size: 10
      idle-timeout: 10000
  jpa:
    show-sql: true
    properties:
      format_sql: true
    hibernate:
      ddl-auto: create